apiVersion: gateway.kgateway.dev/v1alpha1
kind: TrafficPolicy
metadata:
  name: path-rate-limit
  namespace: kgateway-system
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: test-route-1
  rateLimit:
    local: {}  # Added required empty local configuration
    global:
      domain: "api-gateway"
      descriptors:
      - key: "path"
        valueFrom:
          path: true
      extensionRef:
        name: global-ratelimit
      failOpen: false